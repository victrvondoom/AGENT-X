fastapi
uvicorn[standard]
psycopg[binary]>=3.1
psycopg-pool
fastembed
boto3
cryptography
litellm
python-dotenv
pydantic>=2
numpy
jinja2
python-multipart
httpx
fastmcp
# PDF text-layer extraction for uploaded evidence (agentx/documents.py). Pure
# Python, no binary dependency. Optional at runtime: when it is absent, an
# uploaded PDF is stored, hashed and reported as unread rather than failing.
pypdf
