"""
Agent X's consumer resolution engine — a universal consumer resolution agent,
built on this project's existing trust spine.

    agentx.ontology     what consumer problems ARE, declared in YAML
    agentx.understanding a distribution over problem types, not a label
    agentx.evidence     raw artefacts → traceable, contradiction-aware facts
    agentx.policy       deterministic rights analysis, cited
    agentx.eligibility  ranked remedies with named blockers
    agentx.planner      a validated execution graph, not a paragraph
    agentx.governor     what may happen without asking, and why
    agentx.execution    provider-independent actions, recorded and verified
    agentx.followup     case-aware chasing, on a clock
    agentx.chain        the per-case tamper-evident record
    agentx.receipt      a signed, consumer-readable proof of what happened
    agentx.engine       the order those run in

Nothing here replaces the erasure or document pipelines. The erasure cascade,
the audit chain, the confidence gate, the certificate format and the MCP surface
are reused as the trust layer underneath every case.
"""
